apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmCharts.fullname" . }}-test-connection"
  labels:
{{ include "helmCharts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helmCharts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
